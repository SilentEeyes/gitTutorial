***LINUX Command*******
1. pwd : present working directory
2. ls : content list of current directory
3. cd folderName/: change directory 
4. touch fileName : generate a file with fileName
-> git status 
	-> if(not initialize) {
		git init
	}
-> git add --a (staging state)
-> check by git status if you want
-> git commit -m "your message"


-> If you want to check how many and which commits we have done.
	-> git log
	
	
-> if you want to  delete git repository
	-> rm -rf .git
	
-> clone of other directory
	-> git clone "git url" name ifyou want to give
	

	